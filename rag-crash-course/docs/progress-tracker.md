# Progress Tracker — Modern RAG Crash Course

> Source of truth for what's actually done vs. pending. A box is checked **only** after Kamran has explicitly confirmed it — never inferred from context. If you're a fresh Claude session picking this up: trust this file over anything implied elsewhere, and don't mark anything complete without asking first.

**Note (added 2026-07-31) — roadmap sync audit:** after finding Lesson 2.5 missing from this file, Kamran asked for a full pass comparing every lesson heading in `docs/modern-rag-crash-course-roadmap.md` against this tracker, so we don't hit the same surprise mid-lesson again. Full lesson list per the roadmap (13 lessons total, confirmed via every `## ` heading in the file, cross-checked against the intro's own "Verification Notes" and "Month 2 Roadmap" mapping sections): **1, 2, 2.5, 3, 4, 5, 6, 6.5, 7, 8, 9, 10, 11.** Two discrepancies found and fixed at that time: (1) Lesson 6.5 — Query Transformation & Hybrid Retrieval Awareness was entirely missing — added as a placeholder in its correct roadmap position, between Lesson 6 and Lesson 7. (2) Lesson 5's and Lesson 6's titles here were missing the roadmap's own parenthetical suffixes, "(the write path)" and "(the read path)" — corrected. The roadmap's "Month 2" references (e.g. "Lesson 5.2", "Lesson 6.3") are a *different, later, bigger course* this crash course is a foundation for, not additional lessons of this course.

**Note (added 2026-08-15) — re-audited during Lesson 6 pickup:** did a fresh heading-count pass against `docs/modern-rag-crash-course-roadmap.md` before starting Lesson 6 — still 13 lessons, no drift found this time.

**Note (added 2026-08-15) — Lesson 6.6 inserted (Revision 3):** mid-way through Lesson 6.5 (before 6.5.1 had even been confirmed), Kamran asked to build Dense+Sparse/BM25 hybrid search with Reciprocal Rank Fusion for real, instead of leaving it conceptual-only and deferred to the later Month 2 course. Decision, confirmed by Kamran: insert a **new Lesson 6.6 — Hybrid Retrieval Implementation**, between 6.5 and 7, using the same decimal-insert convention already used to slot 6.5 itself in without renumbering anything downstream. The roadmap (`docs/modern-rag-crash-course-roadmap.md`) was updated accordingly: Lesson 6.5's hybrid-retrieval paragraph and concept check were corrected (the original framing — "hybrid is inherently index-level" — was only true for a Pinecone-native rebuild; the RRF-based two-retriever approach this course now builds is pure application code, same as HyDE), and a full new Lesson 6.6 section was added. Lesson list is now **14 lessons: 1, 2, 2.5, 3, 4, 5, 6, 6.5, 6.6, 7, 8, 9, 10, 11.** Lesson 6.5's own scope (HyDE) and its already-drafted 6.5.1 code are unchanged by this — 6.5 is still the active in-progress lesson; 6.6 doesn't start until 6.5 is confirmed complete.

---

## Lesson 1 — Prerequisites & Lean Project Setup — ✅ COMPLETE

- [x] 1.1 Prerequisites confirmed (Python 3.13+ installed, `uv` installed, `OPENAI_API_KEY` obtained, `PINECONE_API_KEY` obtained)
- [x] 1.2 Project initialized (`uv init rag-crash-course --python 3.13`)
- [x] 1.3 Core dependencies installed via `uv add` (`langchain`, `langchain-openai`, `langchain-pinecone`, `langchain-text-splitters`, `pinecone`, `pydantic-settings`, `python-dotenv`)
- [x] 1.4 Dev dependency installed (`uv add --dev ruff`)
- [x] 1.5 Folder skeleton created — `src/rag/{ingestion,indexing,retrieval,generation,evaluation}` (each with `__init__.py`) and `data/docs/`
- [x] 1.6 `.env` created (`OPENAI_API_KEY`, `PINECONE_API_KEY`, `PINECONE_INDEX_NAME`, `EMBEDDING_MODEL`, `CHAT_MODEL`)
- [x] 1.7 `.gitignore` created (before any `git init`)
- [x] 1.8 `src/rag/config.py` written — pydantic-settings `BaseSettings` loading the five `.env` values, exporting a module-level `settings` instance
- [x] 1.9 Six seed markdown docs created under `data/docs/` (`hr_policies.md`, `employee_benefits_leave.md`, `travel_expense_policy.md`, `procurement_guidelines.md`, `product_catalog_faq.md`, `product_catalog_services_guide.md`)
- [x] 1.10 `DOC_METADATA` mapping stubbed in `src/rag/ingestion/loader.py` (each filename → `{"team": ..., "doc_type": ...}`)
- [x] 1.11 Done-When check: `uv run python -c "from rag.config import settings; print(settings.PINECONE_INDEX_NAME)"` runs with no import errors and prints the index name; all six markdown files confirmed present in `data/docs/`

**Note (added 2026-07-26):** 1.1–1.6 were found already physically set up on disk when this session picked up the course (pyproject.toml, uv.lock, .venv, full folder skeleton, and a populated `.env` all present). Kamran explicitly confirmed these as done rather than this being inferred. Full writeup — concepts, decisions, the `uv run` vs. bare `python` bug, exact commands — is in `lesson-notes.md`.

**Note (added 2026-07-31) — Domain pivot:** Kamran requested a full domain swap, replacing the original "DevPortal" knowledge base with a fictional company, **Turab Industries Pvt. Ltd.** (see the updated roadmap). The 1.9 and 1.10 rows above now reflect the *current* state: the original six DevPortal docs (`deploy.md`, `auth.md`, `rate_limits.md`, `on_call.md`, `migrations.md`, `incident_response.md`) have been deleted from `data/docs/` and replaced with the six Turab Industries docs listed in 1.9; `loader.py`'s `DOC_METADATA` was rewritten to match (six markdown entries, plus the two PDF entries — `corporate_gifts_price_list.pdf`, `company_overview.pdf` — already added for real, ahead of Lesson 2.5, since Kamran had both PDFs ready early). `.env`'s `PINECONE_INDEX_NAME` is confirmed set to `turab-industries` (hyphenated, correct). Lesson 1's original build (against the DevPortal domain) genuinely happened and is preserved as-is in `lesson-notes.md`, with its own supersession note added there rather than rewritten.

---

## Lesson 2 — The RAG Mental Model — ✅ COMPLETE

- [x] 2.1 Can explain why RAG exists at all — the fine-tuning vs. RAG tradeoff, and that RAG doesn't make the model "smarter," it gives the model's existing reasoning the right facts to work with
- [x] 2.2 Can explain the six core pipeline concepts in your own words: Document, Chunk, Embedding, Vector index, Retrieval, Augmentation, Generation
- [x] 2.3 Can explain the core relationship: "retrieval quality caps generation quality" — why a perfect prompt can't rescue chunks that don't contain the answer
- [x] 2.4 Can explain **Naive RAG**: the straight-line chunk→embed→retrieve→generate pipeline and its known failure modes (vocabulary mismatch, noisy retrieval, no recovery from a bad first attempt)
- [x] 2.5 Can explain **Advanced RAG**: the two specific upgrade points (pre-retrieval, post-retrieval) and why the pipeline's overall *shape* doesn't change
- [x] 2.6 Can explain **Agentic RAG**: what extra decision-making it adds (whether/how many times/with what query to retrieve) and why this course deliberately doesn't build it
- [x] 2.7 Can recognize **Modular RAG** and **GraphRAG** by name and state what each is for, even though neither is built in this course
- [x] 2.8 Self-check Q1 answered: "if a RAG app confidently answers a question wrong, list the distinct pipeline stages that could be the root cause, and a symptom that isolates each"
- [x] 2.9 Self-check Q2 answered: "why does Advanced RAG's definition specifically split into pre-retrieval and post-retrieval fixes, rather than just being 'any RAG system with more features bolted on'?"

**Note (added 2026-07-31) — closure process for this lesson:** 2.1–2.3 were taught and walked through live in this chat, with Kamran confirming each in turn before moving to the next. 2.4–2.9 were **not** walked through in this conversation — Kamran stated he'd already learned this theoretical material in advance and explicitly asked to mark the rest of the lesson complete and close the chapter. Recorded here rather than silently checked off. See `lesson-notes.md` for the full Lesson 2 entry.

---

## Lesson 2.5 — PDF Ingestion (Tier 1) — ✅ COMPLETE

- [x] 2.5.1 Dependencies installed: `uv add pypdf langchain-community`
- [x] 2.5.2 Confirmed `DOC_METADATA` already has entries for both PDFs — `corporate_gifts_price_list.pdf` → `{"team": "sales", "doc_type": "catalog"}`, `company_overview.pdf` → `{"team": "leadership", "doc_type": "guide"}`
- [x] 2.5.3 `ingestion/loader.py`'s `load_documents()` written — dispatches by file suffix: `.md` files get a plain raw-text read (one Document per file), `.pdf` files load via `PyPDFLoader(path).load()` (one Document per page) with `source` + `DOC_METADATA`'s `team`/`doc_type` merged onto every page-Document without overwriting `PyPDFLoader`'s own `page` field
- [x] 2.5.4 Preview `__main__` block written in `loader.py` — prints, for each of the two PDFs, total page count and the first page's metadata keys
- [x] 2.5.5 Done-When check: `uv run python -m rag.ingestion.loader` ran cleanly — `corporate_gifts_price_list.pdf` loaded 2 pages, `company_overview.pdf` loaded 1 page, every page's metadata included `source`/`team`/`doc_type`/`page`
- [x] 2.5.6 Concept check answered — page-granularity vs. file-granularity and manual-vs-automatic metadata propagation; why suffix-dispatch keeps downstream stages format-agnostic; the Tier 1 vs. Tier 2/3 boundary

**Note (added 2026-07-31):** this lesson was missing from the tracker entirely until caught mid-course (see Lesson 3's note below and the roadmap-sync note at the top of this file). Its build required writing `load_documents()` from scratch — the roadmap's own wording assumed a prior "Lesson 1 raw-read logic" that, on inspection, was never actually built (Lesson 1 only ever stubbed `DOC_METADATA`). A real `langchain-community` deprecation warning surfaced while running the Done-When check and was researched live rather than ignored — see `lesson-notes.md` for what was found. The concept check needed two clarification passes on Q1 (page-granularity vs. chunking's automatic metadata copy — an early explanation wrongly implied metadata copying was markdown-only) and a full plain-language re-explanation on Q3 (Tier 1/2/3), after which Kamran gave direct feedback that explanations should be simpler and less jargon-heavy going forward — teaching style adjusted accordingly from this point on. Full Q&A now also captured in `docs/Interview_QA.md`, started with this lesson.

---

## Lesson 3 — Documents → Chunks — ✅ COMPLETE

- [x] 3.1 `ingestion/chunker.py` written — `chunk_documents(docs)` using `RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=75, separators=["\n\n", "\n", ". ", " ", ""])`, propagating each parent `Document`'s existing metadata onto every chunk automatically, plus adding a new, deterministic `chunk_id` field (`f"{source}::{index}"`)
- [x] 3.2 Ingest preview script written (`run_ingest_preview.py`, placed at the project root — not under `src/rag/`, to keep dev entry-point scripts separate from importable library code) — calls `load_documents()` (from Lesson 2.5) then `chunk_documents()`, and prints the total chunk count plus, for the longest document, how many chunks it produced and their character lengths
- [x] 3.3 Done-When check #1: preview script run — chunk count reasonable (57 chunks across 9 loaded Document objects / 8 source files), every chunk's metadata confirmed (via a follow-up one-off inspection, since the preview script only prints lengths) to include `source`/`team`/`doc_type`/`chunk_id`, and the three suspiciously tiny chunks (26/24/21 chars) inspected directly and confirmed to be complete markdown headings, not sentences cut off mid-thought
- [x] 3.4 `ingestion/parent_child_demo.py` written — standalone, **not** wired into indexing — `build_parent_child(docs)` (parent splitter `chunk_size=900/overlap=100`, child splitter `chunk_size=180/overlap=20`, `parent_id` linking child → parent) plus a `compare(term, ...)` function (whitespace-stripped, case-insensitive substring matching — not real vector search, since embeddings don't exist until Lesson 4) and a `__main__` block running it for the term `"steel water bottle"` against `corporate_gifts_price_list.pdf`
- [x] 3.5 Done-When check #2: `parent_child_demo.py` run for `"steel water bottle"` — recursive chunking returned 2 overlapping matches (499/492 chars); parent-child returned 1 clean child match (173 chars) linked to its parent (880 chars). Output **diverged** from the roadmap's predicted description — the linked parent did not include the price table's column-header row — analyzed and accepted as the actual finding rather than re-tuned to force a match to the prediction (see `lesson-notes.md` for the full analysis)
- [x] 3.6 Concept check answered: why chunk size is a tradeoff and not a constant; why metadata must be attached before splitting, not after; what "recursive" actually means in `RecursiveCharacterTextSplitter` (a priority-ordered fallback through separators, not recursion over documents); when parent-child chunking earns its extra storage-layer cost over recursive chunking alone, and what specifically breaks without it (tying back to the price-list comparison)

**Note (added 2026-07-31):** the original roadmap listing for Lesson 3's "What To Build" mentions `loader.py` as one of its outputs — that work is already done, as of Lesson 2.5 (see above), so it does not appear as a Lesson 3 checklist item here. Lesson 3 only needs `chunker.py` and `parent_child_demo.py`, both consuming `load_documents()`'s already-complete output.

**Note (added 2026-08-02) — closure:** 3.3 passed on all three literal criteria, but surfaced a real, named limitation rather than a clean pass — recursive chunking strands markdown headings alone in their own tiny chunks purely due to character-count boundary luck (not a code bug), which directly motivated 3.4. 3.5's actual output diverged from the roadmap's predicted description: the parent-child demo's linked parent (900 chars) still failed to capture the price table's column-header context, even though widening the window was specifically meant to prevent that — the honest finding is that parent-child *reduces* the risk of losing structural context, it doesn't *guarantee* fixing it, since the parent splitter is still character-count-driven, not table-aware. This became the basis for 3.6 Q4's corrected answer. 3.6's concept check needed corrections on all four questions — mechanisms were generally right, but Q1 overstated "bigger chunks are always better for generation" (oversized chunks dilute both retrieval and generation), Q2's mechanism needed sharpening (metadata must exist before splitting because `split_documents()` copies it forward, it doesn't invent it), Q3 had a self-contradicting first sentence (recursion is over the separator list within one document, not "over the docs/corpus"), and Q4 needed grounding in the actual demo evidence rather than a generic description of the parent-child pattern. All four corrections reviewed and confirmed by Kamran before this lesson was marked complete. Full writeup in `lesson-notes.md`; final corrected Q&A in `docs/Interview_QA.md`.

---

## Lesson 4 — Embeddings: The Vector Space Mental Model — ✅ COMPLETE

- [x] 4.1 Dependency installed: `uv add numpy` (new for this lesson — not part of Lesson 1's original dependency set)
- [x] 4.2 `ingestion/embedding_sanity_check.py` written — instantiates `OpenAIEmbeddings(model=settings.EMBEDDING_MODEL)`, reusing the `EMBEDDING_MODEL` value already defined in `config.py` since Lesson 1
- [x] 4.3 Three sentences defined — two semantically similar paraphrases of each other (Turab Industries domain: HR, procurement, or product catalog — Kamran's own wording, not copied from the roadmap's example) plus one unrelated third sentence from the same domain
- [x] 4.4 `cosine_sim(a, b)` helper written using `numpy` (dot product over the product of the two vectors' norms) — no external similarity library
- [x] 4.5 All three pairwise similarity scores computed and printed with labels — the similar-pair score, and each similar sentence's score against the unrelated one
- [x] 4.6 Plain `assert` added (not a test framework) confirming the two similar sentences score higher against each other than either does against the unrelated sentence, with a printed message
- [x] 4.7 Done-When check: script runs cleanly, prints all three similarity scores, and the assertion passes
- [x] 4.8 Concept check answered: why embedding model consistency between index-time and query-time is non-negotiable, and why that failure mode is silent rather than a raised error; why cosine similarity specifically (not Euclidean distance) is the right default for text embeddings

**Note (added 2026-08-02):** standalone script, ~15 minutes per the roadmap — nothing gets persisted or wired into the real pipeline yet. This is prerequisite mental-model knowledge for indexing (Lesson 5) and retrieval (Lesson 6), not a pipeline stage of its own.

**Note (added 2026-08-02) — closure:** 4.3's sentence pair (HR notice-period policy, paraphrased) produced a real, unpredicted finding: the two "unrelated" comparisons weren't equal (0.5645 vs. 0.4183) even though both are equally off-topic from the similar pair — flagged as an observation about embeddings picking up on structural/numeric phrasing similarity, not just topic, and fed directly into 4.8's discussion. 4.8's concept check was right on the core mechanism for both questions on the first attempt, but needed one sharpening each, not a correction: Q1 needed the distinction between a **dimension mismatch** (different vector sizes → hard crash in the similarity math) and a **same-dimension, different-model mismatch** (the truly silent failure — math runs, result is meaningless), tied back to why changing `EMBEDDING_MODEL` requires a full re-index, not just a config edit. Q2 needed the practical note that OpenAI's embeddings are already unit-normalized, so cosine similarity and Euclidean distance are mathematically equivalent for them specifically (`Euclidean² = 2 − 2×cosine`) — cosine remains the safer default because it doesn't depend on that guarantee holding for every model. Both additions reviewed and confirmed by Kamran before the lesson was marked complete. Full writeup in `lesson-notes.md`; final Q&A in `docs/Interview_QA.md`.

---

## Lesson 5 — Vector Store & Indexing (the write path) — ✅ COMPLETE

- [x] 5.1 `indexing/embed_and_index.py` created — `EMBEDDING_DIMENSIONS` dict defined (`{"text-embedding-3-small": 1536, "text-embedding-3-large": 3072}`), keyed by model name so index dimension is always derived, never hardcoded
- [x] 5.2 `get_or_create_index(pc: Pinecone) -> None` written — checks `pc.has_index(settings.PINECONE_INDEX_NAME)` first (idempotent — safe to call on every run), creates a serverless index only if missing, with `dimension` looked up from `EMBEDDING_DIMENSIONS[settings.EMBEDDING_MODEL]`, `metric="cosine"`, `spec=ServerlessSpec(cloud="aws", region="us-east-1")`
- [x] 5.3 `index_chunks(chunks: list[Document]) -> PineconeVectorStore` written — builds `OpenAIEmbeddings(model=settings.EMBEDDING_MODEL)`, calls `PineconeVectorStore.from_documents(documents=chunks, embedding=embeddings, index_name=settings.PINECONE_INDEX_NAME, ids=[c.metadata["chunk_id"] for c in chunks])` — explicit `ids=` from the deterministic `chunk_id` (from Lesson 3) is what makes re-ingestion overwrite instead of duplicate
- [x] 5.4 `run_ingest.py` entrypoint written at the project root — resolved via explicit decision (not the roadmap's suggested `src/rag/run_ingest.py`), matching Lesson 3's convention of keeping dev entry-point scripts at the root, separate from importable library code under `src/rag/`. Chains `load_documents()` → `chunk_documents()` → `index_chunks()`, then prints the resulting vector count via `pc.Index(name).describe_index_stats()`
- [x] 5.5 Done-When check #1: `uv run python run_ingest.py` completed with no errors — `describe_index_stats()` reported 57 vectors, matching Lesson 3's chunk count exactly (57 chunks, from 9 loaded `Document` objects / 8 source files). Independently confirmed both via terminal output and by Kamran seeing the 57 vectors directly in the Pinecone console
- [x] 5.6 Done-When check #2 (idempotency): same ingestion run a second time immediately after — vector count held at 57, did not double to 114, proving the `ids=` deterministic-ID strategy works as intended
- [x] 5.7 Concept check answered: why index dimension must exactly match the embedding model's output size, and what concretely happens if it doesn't; why a deterministic vector ID is a production requirement and not just a nicety; what a Pinecone namespace is and when you'd reach for one instead of a metadata filter

**Note (added 2026-08-02):** `pinecone` and `langchain-pinecone` are already installed — both were part of Lesson 1's original `uv add` list, so there's no new-dependency item this lesson (unlike Lesson 2.5's `pypdf` or Lesson 4's `numpy`). This lesson is the pipeline's **write path** — Lesson 6 (the **read path**) depends on the metadata this lesson upserts, since Lesson 6's filtering can't work on metadata that was never indexed.

**Note (added 2026-08-15) — closure:** a real file-identity mix-up surfaced during 5.5's Done-When check — 5.4's `run_ingest.py` code had been pasted into the pre-existing `run_ingest_preview.py` file instead of a new file, overwriting Lesson 3's original preview script content in the process. Caught because the terminal screenshot showed the command `run_ingest_preview.py` while the printed output ("Ingested N chunks." / "Index '...' now reports N vectors.") only matches what `run_ingest.py`'s code produces — Lesson 3's original preview script never touched Pinecone at all. Resolved by Kamran's explicit choice: renamed the file to `run_ingest.py` via `mv` rather than recreating a separate preview script, since Lesson 3's preview script had already done its job and is fully documented in `lesson-notes.md` — it doesn't need to keep existing on disk once superseded. 5.7's concept check went well: Q1 and Q2 were both correct on the first attempt (Q1 independently tied back to Lesson 4's dimension-mismatch-vs-same-dimension-different-model distinction without prompting) and only needed sharpening, not correction. Q3 (Pinecone namespaces) was genuinely new material never built in this lesson's code — Kamran correctly guessed the core idea (multi-tenant isolation, a default namespace exists) while explicitly flagging he hadn't worked with it directly, and it was taught fully from there. All three reviewed and confirmed before the lesson was marked complete. Full writeup in `lesson-notes.md`; final Q&A in `docs/Interview_QA.md`.

---

## Lesson 6 — Retrieval & Metadata Filtering (the read path) — ✅ COMPLETE

- [x] 6.1 `retrieval/retriever.py` created — `get_vector_store() -> PineconeVectorStore` written — reconnects to the existing Pinecone index using `OpenAIEmbeddings(model=settings.EMBEDDING_MODEL)` and `settings.PINECONE_INDEX_NAME` (does **not** re-create the index or re-upsert anything — that's Lesson 5's job)
- [x] 6.2 `retrieve(vector_store, query: str, k: int = 5, team: str | None = None, doc_type: str | None = None) -> list[tuple[Document, float]]` written — builds a Pinecone `filter` dict using `{"$eq": ...}` only for whichever of `team`/`doc_type` are actually provided, calls `vector_store.similarity_search_with_score(query, k=k, filter=filter_dict or None)`
- [x] 6.3 `run_retrieval_demo.py` written at the project root (same entry-point convention Kamran chose in Lesson 5.4 — nothing imports this file, so it lives at root, not under `src/rag/`, despite the roadmap suggesting `src/rag/run_retrieval_demo.py`) — runs the query `"what is the notice period for resigning?"` three times: no filter, `team="hr"`, `team="sales"` — prints each result set's chunk sources and scores side by side
- [x] 6.4 Done-When check: demo run — the unfiltered query returns `hr_policies.md`'s chunk as the top hit; the `team="sales"`-filtered query returns either zero results or a clearly different, clearly-wrong-for-this-question result set — proving the filter is actually constraining the search space, not just re-ranking within the same results
- [x] 6.5 Concept check answered: why metadata filtering happens at the vector-search layer instead of as a post-hoc Python `if` on results; why a raw cosine similarity score is not a calibrated confidence value; the two-stage "wide retrieval, then narrow" pattern and why it's different from just picking a smaller `k`

**Note (added 2026-08-15):** no new dependencies this lesson — `langchain-pinecone`, `langchain-openai`, and `pinecone` are all already installed (Lesson 1/5). Per the roadmap, `run_retrieval_demo.py` is specified at `src/rag/run_retrieval_demo.py`, but this tracker places it at the project root instead, applying the root-for-entry-points/`src/rag`-for-library-code convention as a settled project-wide rule now, not re-litigated per lesson. This lesson is the pipeline's **read path** — it depends directly on Lesson 5's write path having actually populated the index with the `team`/`doc_type` metadata Lesson 1/2.5 attached at loading time.

**Note (added 2026-08-15) — closure:** 6.1 confirmed via direct terminal proof — `get_vector_store()` printed a clean `PineconeVectorStore` object, no errors. 6.2 confirmed by explicit "Done" after code review, with real verification deferred to 6.3/6.4's full run since `retrieve()` has no meaningful standalone output without an actual query. 6.3/6.4's demo run passed both literal Done-When criteria: unfiltered top hit was `hr_policies.md` (score 0.7372); `team="sales"` returned a completely different, much lower-scoring result set (product catalog / gift price-list chunks, best score 0.2244) — proving the filter genuinely narrows the candidate pool before scoring, not just re-sorting the same list. A real, unplanned observation surfaced here, not a bug: `team="hr"` returned results identical to the unfiltered run, because all five of the unfiltered top-5 already happened to be tagged `team="hr"` — the filter did apply, it simply had nothing left to exclude in that specific run. 6.5's concept check: Q1 (filter at vector-search layer) and Q3 (wide-then-narrow vs. smaller `k`) were both correct on the first attempt, needing only minor additions (efficiency-at-scale + access-control-as-enforcement-point for Q1; the "two different tools for two different jobs" framing for Q3). Q2 (raw cosine score ≠ calibrated confidence) needed a real correction — the first answer correctly said "low score = not very relevant" but didn't explain *why* the number isn't calibrated; corrected to: cosine similarity is just the angle between two vectors, never trained against ground-truth correctness, so there's no universal scale tying a number to "% correct" — scores are only meaningful compared *within* one query, not in isolation. A genuine, unscripted follow-up question also came up mid-concept-check: whether production filters are automatic or manually defined like the demo's. Answered in full: this lesson's filters are caller-supplied (matching the roadmap's own "Deferred" note for Lesson 6); real production derives the filter value server-side from the authenticated user's identity/session, which is explicitly distinct from a second, different kind of "automatic" filtering — inferring team/doc_type from the query's own content, a relevance technique (Lesson 6.5 territory), not a security one. All corrections reviewed and explicitly confirmed by Kamran before the lesson was marked complete. Full writeup in `lesson-notes.md`; final Q&A in `docs/Interview_QA.md`.

---

## Lesson 6.5 — Query Transformation & Hybrid Retrieval Awareness — ✅ COMPLETE

- [x] 6.5.1 `hyde_query(question: str) -> str` added to `retrieval/retriever.py` — uses a module-level `_hyde_llm = ChatOpenAI(model=settings.CHAT_MODEL, temperature=0.3)`, prompts the LLM to write a short, plausible-sounding paragraph that *would* answer the question, in the style of an internal Turab Industries policy doc — explicitly allowed to be factually wrong, since only vocabulary/style match matters for this technique
- [x] 6.5.2 `retrieve()` (from Lesson 6) extended with a new `query_transform: str = "none"` parameter — when `"hyde"`, calls `hyde_query(query)` first and embeds/searches with *its* output instead of the raw query string; when `"none"` (the default), behavior is identical to Lesson 6 — no existing caller's behavior changes
- [x] 6.5.3 `run_hyde_demo.py` written at the project root (same entry-point convention as `run_retrieval_demo.py`) — runs one deliberately short/vague question (`"what do I do if I need to travel for a client meeting on short notice?"`) through `retrieve()` twice — once `query_transform="none"`, once `query_transform="hyde"` — prints both runs' top chunk source and score side by side
- [x] 6.5.4 Done-When check: demo run shows a visible difference between the two runs on the vague question — same top chunk (`travel_expense_policy.md`) in both runs, score moved `0.5195` → `0.6222`, satisfying the "same chunk, meaningfully different score" pass condition
- [x] 6.5.5 Concept check answered: why HyDE's hypothetical answer improves retrieval *despite* possibly being factually wrong (vocabulary/style matching, not correctness); why a hypothetical answer counts as "document language" even when its facts are invented, and why a raw user question usually doesn't

**Note (added 2026-08-15):** no new dependencies — `ChatOpenAI` comes from `langchain-openai`, already installed since Lesson 1 (previously only `OpenAIEmbeddings` from that same package had been used; this is a new class from an existing dependency, not a new package). Per the roadmap, this lesson is the **pre-retrieval** half of Advanced RAG (Lesson 10's reranking is the post-retrieval half). Only **HyDE** is actually implemented here — **Query Expansion** is explained conceptually (mechanism, tradeoffs, expected gains) but deliberately *not* built, since the Turab Industries corpus is small enough that single-query retrieval already performs well; Month 2 builds it for real. **Dense+Sparse/BM25 hybrid search with Reciprocal Rank Fusion** was originally scoped the same way (conceptual only) but is **no longer deferred** — see the 2026-08-15 "Lesson 6.6 inserted" note above; it now gets its own dedicated hands-on lesson immediately after this one, at Kamran's request.

**Note (added 2026-08-15) — closure:** 6.5.4's demo run surfaced a genuine, unscripted finding: the `query_transform="none"` baseline already retrieved the *correct* document (`travel_expense_policy.md`) for the deliberately vague test question — the question's own wording ("travel," "client meeting," "short notice") already overlapped enough with a *travel* policy doc that this wasn't a severe enough vocabulary gap to send the baseline fully off-target. HyDE's demonstrated contribution here was **sharpening confidence** on an already-correct retrieval (`0.5195` → `0.6222`), not rescuing a wrong one — accepted and recorded as the real finding rather than treated as a weaker or failed result; full analysis in `lesson-notes.md`. 6.5.5's concept check: both questions correct on the core mechanism on the first attempt, each needing one sharpening, not a correction — Q1 needed the explicit reason the hypothetical is never shown to the user or fact-checked; Q2 needed the imprecise "what embeddings expect from training data" phrasing replaced with the more precise **linguistic register** framing (docs = declarative/expository, questions = short/interrogative), plus softening "questions never count as document language" to "usually not." Both sharpenings reviewed and explicitly confirmed by Kamran before the lesson was marked complete. Full writeup in `lesson-notes.md`; final Q&A in `docs/Interview_QA.md`.

## Lesson 6.6 — Hybrid Retrieval Implementation (Dense + Sparse/BM25 + Reciprocal Rank Fusion) — ✅ COMPLETE

*(Inserted 2026-08-15 via Revision 3, at Kamran's request, ahead of Month 2 — see this file's 2026-08-15 "Lesson 6.6 inserted" note and `docs/modern-rag-crash-course-roadmap.md`'s own Revision 3 note for the full reasoning.)*

- [x] 6.6.1 Dependency installed: `uv add rank-bm25` (deliberately not `langchain-community`'s `BM25Retriever` wrapper — that package was already flagged as being sunset back in Lesson 2.5, and `rank-bm25` is the small, dependency-light library it wraps anyway)
- [x] 6.6.2 `retrieval/sparse_retriever.py` created — `build_bm25_index(chunks: list[Document]) -> tuple[BM25Okapi, list[Document]]`, tokenizing each chunk's `page_content` with a simple `.lower().split()` and returning the fitted index alongside the chunks list in the same order, so a BM25 result position maps back to its `Document`
- [x] 6.6.3 `sparse_retrieve(bm25_index, chunks, query: str, k: int = 10) -> list[Document]` written in `sparse_retriever.py` — tokenizes the query the same way, scores every chunk via `bm25_index.get_scores(...)`, returns the top-k `Document`s by score, no metadata filtering (BM25 never touches Pinecone)
- [x] 6.6.4 `retrieval/fusion.py` created — `reciprocal_rank_fusion(ranked_lists: list[list[Document]], k: int = 60) -> list[Document]`, keyed on each `Document`'s `chunk_id` so a chunk appearing in both lists has its scores summed (`Σ 1/(k+rank)`), not counted twice, returning Documents sorted by fused score descending
- [x] 6.6.5 `hybrid_retrieve(vector_store, bm25_index, chunks, query: str, k: int = 5) -> list[Document]` added to `retriever.py` — calls the existing dense `retrieve()` and the new `sparse_retrieve()` independently (each at a wider `k`, e.g. 10), fuses both ranked lists with `reciprocal_rank_fusion`, returns the top-k fused result; does not modify `retrieve()`'s own signature or default behavior, and does not touch the Pinecone index's metric or schema
- [x] 6.6.6 `run_hybrid_demo.py` written at the project root (same entry-point convention as `run_retrieval_demo.py`/`run_hyde_demo.py`) — builds the BM25 index once from `chunk_documents(load_documents())`, then runs **two** queries — one containing an exact token pulled verbatim from `corporate_gifts_price_list.pdf` (expected to favor BM25), one paraphrased/vague HR-style question (expected to favor dense) — through dense-only, sparse-only, and hybrid retrieval, printing all three ranked lists (chunk source + rank) side by side per query
- [x] 6.6.7 Done-When check: demo run showed real, visible differences between modes on both queries; identical output on a second run confirmed BM25 + RRF are fully deterministic (unlike HyDE, which depends on an LLM call)
- [x] 6.6.8 Concept check answered: why RRF operates on rank position, not raw score, and why that specifically is what lets you fuse two lists on completely different, incomparable scales; why this lesson's two-independent-retrievers-plus-RRF approach is *not* an index-level change, unlike Pinecone-native hybrid, and what you give up by choosing this path; what kind of query BM25 is expected to win on versus what kind dense is expected to win on, grounded in the actual demo evidence

**Note (added 2026-08-15) — closure:** 6.6.7's demo run surfaced a real, named finding, not just a clean pass: on Query 1 (`"steel water bottle"`), BM25's result list was visibly degraded by a zero-score tiebreak artifact — chunks with zero matching tokens (e.g. `hr_policies.md`) ranked *above* other genuine `corporate_gifts_price_list.pdf` chunks purely because Python's stable sort preserves original corpus-load order among ties, and `hr_policies.md` loads first. Hybrid (RRF) visibly reduced but did not fully eliminate this contamination — dense's clean signal outweighed but didn't erase BM25's noise. On Query 2 (the paraphrase question), the prediction held cleanly: dense swept all 5 slots, BM25 visibly wandered into off-topic documents, and hybrid matched dense exactly. Determinism confirmed via an identical second run. 6.6.8's concept check: Q1 correct on the first attempt (no correction, only an added connection to why RRF needs no tuned weights — nothing left to weight once you're only using rank). Q2's tradeoffs (two queries instead of one, no server-side alpha tuning) were correct, but the "index-level" framing was sharpened: the deciding factor isn't index *count*, it's whether you have to migrate/modify your *existing* storage layer's schema or metric — our BM25 "index" isn't real persisted infrastructure at all (in-memory, free, rebuilt every process start), unlike a Pinecone-native hybrid rebuild. Q3 needed a real correction, not a sharpening: the first answer was accurate BM25-vs-dense *theory* but didn't ground itself in the actual demo evidence as asked — the real evidence showed Query 2 cleanly confirmed "dense wins on paraphrase," but Query 1 did *not* cleanly confirm "BM25 wins on exact tokens" (dense matched BM25's result quality there too, and BM25's own list was degraded by the tiebreak artifact) — a more honest, more interesting finding than the clean textbook split. **Kamran explicitly instructed marking the whole lesson complete at this point, without providing a re-answered Q3 in this conversation** — recorded here explicitly as a deliberate deviation from the course's normal live-correction-and-reconfirm flow, the same way Lesson 2's early closure was recorded rather than silently treated as a standard pass. Full writeup in `lesson-notes.md`; final (corrected) Q&A in `docs/Interview_QA.md`.

## Lesson 7 — Prompt Augmentation & Generation — ✅ COMPLETE

*(Granular checklist prepared 2026-08-15, immediately after Lesson 6.6's closure, per the standing rule that the next lesson's checklist is ready before it begins. Started and completed 2026-08-19.)*

- [x] 7.1 `generation/prompt.py` created — `SYSTEM_PROMPT` (grounding instruction for a Turab Industries HR & operations assistant persona: answer ONLY from the numbered sources, say "I don't have that in the knowledge base" instead of guessing if the sources don't contain the answer, cite sources inline like `[1]`), plus a `ChatPromptTemplate` built via `from_messages([("system", SYSTEM_PROMPT), ("human", "Sources:\n{context}\n\nQuestion: {question}")])`
- [x] 7.2 `generation/chain.py`: `format_docs(scored_docs: list[tuple[Document, float]]) -> str` written — numbers each source (`[1]`, `[2]`, ...) and labels it with its metadata `source` field, joining into one string for the `{context}` slot
- [x] 7.3 `generation/chain.py`: `answer_question(vector_store, question: str, k: int = 5, team: str | None = None, doc_type: str | None = None) -> dict` written — calls `retrieve()` from Lesson 6, formats the docs via `format_docs()`, invokes the `prompt | ChatOpenAI(model=settings.CHAT_MODEL, temperature=0) | StrOutputParser()` LCEL chain, returns `{"answer": str, "sources": [...], "raw_chunks": scored_docs}` — deliberately returns the raw retrieved chunks alongside the answer, not just the final text
- [x] 7.4 `__main__` demo written asking three questions: one clearly answerable from the docs ("How many earned leave days do I accrue per month?"), one clearly NOT in the docs ("What was Turab Industries' revenue last year?" — verifying refusal instead of a fabricated answer), and one spanning two source documents ("If I'm still on probation, what's my notice period, and do I have health insurance yet?" — `hr_policies.md` + `employee_benefits_leave.md`). **Revised mid-build**: `answer_question()`'s `"sources"` field was changed from "every unique file across all retrieved chunks" to "only files whose `[N]` citation marker literally appears in the generated answer text" — see note below.
- [x] 7.5 Done-When check: all three demo questions produce sensible output — the answerable one cites the right source, the unanswerable one triggers the "I don't have that" refusal rather than a fabricated answer, and the cross-document one cites more than one source
- [x] 7.6 Concept check answered: why the grounding instruction is the single highest-leverage sentence in the whole prompt; what "lost in the middle" means and why it's a context-ordering problem, not a model-capability problem; why returning `raw_chunks` alongside the answer (not just the final string) is a production requirement

**Note (added 2026-08-19) — mid-lesson fix, recorded early at Kamran's explicit request (deviation from the standing "sync docs only at lesson end" rule; full retrospective still deferred to `lesson-notes.md` at 7.6's close):** the first 7.4 demo run exposed a real bug — `"sources"` was built from every unique file among all `k=5` retrieved chunks, not from what the model actually cited. Q1 ("earned leave days") answered correctly and cited only `[1]`, but `Sources:` showed three files, including `travel_expense_policy.md`, which the answer never used. Q2 (revenue — refused correctly) still showed three files in `Sources:` despite citing nothing. Fixed by checking, per retrieved chunk, whether its `[i+1]` marker literally appears in the generated `answer` string, and only including that chunk's `source` if so — re-run confirmed Q1 now shows `['employee_benefits_leave.md']` only, Q2 now shows `[]`, Q3 unchanged (`['employee_benefits_leave.md', 'hr_policies.md']`, both genuinely cited). Known limitation of the fix, not yet hit in practice: it's a literal substring check for `"[N]"`, so a combined citation format like `"[1, 2]"` (instead of `"[1][2]"`) would go undetected — considered acceptable given `temperature=0` and the explicit "cite like `[1], [2]`" instruction make that format unlikely, not impossible.

A second real finding surfaced while debugging Q3's `[3]`-not-`[1]` citation (Kamran's question, not something anticipated): direct inspection of Q3's `raw_chunks` showed the *highest-cosine-scoring* chunk (`[1]`, `hr_policies.md`, score 0.4469, about probation *duration* — 90 days, extendable) was retrieved but never cited, while lower-scoring chunks `[2]` (0.3942, health insurance) and `[3]` (0.3555, notice period) were the ones actually used. Confirms concretely — not just theoretically — Lesson 6.5's "cosine score is not a calibrated confidence value" point: `[1]` scored highest almost certainly on vocabulary overlap with "probation" in the question, but didn't contain either fact the question actually needed. This is also the clearest evidence yet for *why* the `sources`-filtering fix above matters: without it, `travel_expense_policy.md`-style noise (a retrieved-but-unused, topically-adjacent chunk) would keep leaking into a field meant to reflect what backed the answer.

**Note (added 2026-08-19) — closure:** 7.6's concept check — three questions, all correct on the first attempt, each needing one sharpening, not a correction. Q1 (grounding instruction): needed the specific reason hallucination-under-confident-tone is dangerous — a fabricated answer reads with the exact same fluency and confidence as a grounded one, so there's no surface-level tell without manually checking the cited source; that's what makes one sentence buying protection against it "highest-leverage." Q2 (lost in the middle): needed the diagnostic proof that it's positional, not comprehension-based — the same model, same weights, recalls the same fact correctly once it's repositioned to the front or end of the same context — plus the tie-back to why context-ordering (most relevant chunk closest to the question) is this lesson's real mitigation, and why it's a non-issue yet at this project's k=5. Q3 (`raw_chunks`): needed the root-cause-disambiguation framing — without `raw_chunks`, a wrong or refused answer can't be diagnosed as a retrieval failure vs. a generation failure, tying back to Lesson 2's "check retrieval before touching the prompt" debugging order — plus the secondary production use (a real citation UI consumes `raw_chunks` too, not just developer debugging). All three sharpenings reviewed and explicitly confirmed by Kamran before the lesson was marked complete. See the notes above for the mid-lesson `sources`-filtering bug/fix and the Q3 chunk-ranking finding that surfaced from debugging it — both are part of this lesson's real build history, not just its concept check. Full writeup in `lesson-notes.md`; final Q&A in `docs/Interview_QA.md`.

## Lesson 8 — Wiring It Together: End-to-End RAG API — ✅ COMPLETE

*(Granular checklist prepared 2026-08-19, immediately after Lesson 7's closure. Started and completed 2026-08-22.)*

- [x] 8.1 Dependency installed: `uv add "fastapi[standard]"`
- [x] 8.2 `api/main.py` created — `FastAPI(title="Turab Industries RAG API")` app instance, plus the vector store connection (`get_vector_store()` from Lesson 6) instantiated ONCE at module load time, not inside a route handler
- [x] 8.3 `GET /health` endpoint added — returns `{"status": "ok"}`, no dependencies on the vector store or any other pipeline stage
- [x] 8.4 `POST /ingest` endpoint written — re-runs `load_documents()` → `chunk_documents()` → `index_chunks()` (Lessons 2.5/3/5) on demand, returns `{"chunks_indexed": len(chunks)}`
- [x] 8.5 `POST /query` endpoint written — Pydantic `QueryRequest` model (`question: str`, `k: int = 5`, `team: str | None = None`, `doc_type: str | None = None`), calls `answer_question()` from Lesson 7's `chain.py`, returns `{"answer": ..., "sources": ...}` only — deliberately NOT `raw_chunks`, to keep the wire response lean (that field stays an internal/debugging concern, not a public API concern)
- [x] 8.6 Done-When check: `uv run fastapi dev src/rag/api/main.py` starts the server; `POST /ingest` returns a chunk count; `POST /query` with `{"question": "how many earned leave days do I get per year?"}` returns a grounded answer citing `employee_benefits_leave.md`
- [x] 8.7 Concept check answered: why the vector store connection is instantiated once at startup, not per-request; why `POST /ingest` as a synchronous request/response endpoint is a demo simplification, and what a real production ingestion trigger looks like instead

**Note (added 2026-08-22) — closure:** 8.4's first Done-When run surfaced a real, caught-before-confirming bug: the route initially returned `{"chunk_indexed": 57}` (singular) instead of the spec'd `{"chunks_indexed": 57}` — spotted by inspecting the actual response body against the checklist wording rather than just checking for a 200. Fixed and re-verified before 8.4 was marked done. 8.5/8.6 then surfaced a second, more interesting issue: testing via Swagger's `/docs` "Try it out" UI left the optional `team`/`doc_type` fields at their auto-filled placeholder value `"string"` instead of clearing them, which silently became a real Pinecone metadata filter (`team == "string"`) matching zero chunks — every query came back "I don't have that in the knowledge base," including ones with a real answer in the corpus. Root cause found by checking `raw_chunks` first (printed in the terminal log, though deliberately excluded from the public `/query` response by 8.5's own design) — confirming Lesson 2's debugging order (check retrieval before touching anything else) still holds. A second, genuinely interesting finding surfaced from the same broken run: asking "what is the company name?" with zero retrieved chunks still returned a confident, cited-looking answer ("Turab Industries [1]") — the model pulled this from its own persona description in `SYSTEM_PROMPT`, not from grounded context, and fabricated a `[1]` pointing at nothing. Once the filter fields were cleared and retrieval worked normally, the same question came back properly grounded (`sources: ["company_overview.pdf"]`, correct `[2]` citation) — confirming the leak was specific to the empty-context case, not a standing bug. Full writeup in `lesson-notes.md`; final Q&A in `docs/Interview_QA.md`.

## Lesson 9 — Retrieval Evaluation — ✅ COMPLETE

*(Granular checklist prepared 2026-08-22, immediately after Lesson 8's closure. Started and completed 2026-08-22.)*

- [x] 9.1 Dependency installed: `uv add ragas`
- [x] 9.2 `evaluation/eval_dataset.py` created — `QA_PAIRS: list[dict]`, 8 hand-written `{"question": ..., "ground_truth": ...}` pairs against the Turab Industries corpus, spanning all eight source docs (six markdown + two PDFs), with each `ground_truth` written by Kamran reading the actual source doc directly — not invented or guessed. Every one of the 8 independently cross-checked against the real source files and confirmed accurate.
- [x] 9.3 `evaluation/run_eval.py`: `build_eval_dataset(vector_store, qa_pairs) -> EvaluationDataset` written — for each pair, calls `answer_question()` (Lesson 7's `chain.py`, the *same* function the live API uses), assembling a row with `user_input`, `response`, `retrieved_contexts` (from `result["raw_chunks"]`'s page content), and `reference` (the hand-written `ground_truth`)
- [x] 9.4 `evaluation/run_eval.py`: `evaluate()` call wired up — `metrics=[ContextPrecision(), ContextRecall(), Faithfulness(), ResponseRelevancy()]`, evaluator LLM/embeddings built explicitly from `settings` (not left to Ragas's default), results printed as a labeled summary with a one-line interpretation per metric
- [x] 9.5 `__main__` block written — connects via `get_vector_store()` (Lesson 6), loads `QA_PAIRS` from `eval_dataset.py`, runs `build_eval_dataset()` then `evaluate_pipeline()`, prints the summary
- [x] 9.6 Done-When check: `uv run python -m rag.evaluation.run_eval` completed cleanly on the original 8-question set and printed all four scores — `context_precision 0.885`, `context_recall 1.000`, `faithfulness 0.944`, `answer_relevancy 0.907`. Faithfulness specifically checked and confirmed high, consistent with Lesson 7's grounding instruction working as intended.
- [x] 9.7 Concept check answered: why Faithfulness and Context Recall are separate metrics with different fixes; why the eval harness must call the actual pipeline functions to be trustworthy; what a low-context-precision-with-high-recall combination means for this pipeline. **Marked complete per Kamran's explicit instruction without a re-confirmed answer** — see closure note below.
- [x] 9.8 (extension, beyond original scope) Modern, non-deprecated `ragas.metrics.collections` implementation added in a new file, `evaluation/run_eval_modern.py` — `evaluate_pipeline_modern(dataset)`, an async per-row scoring loop using `llm_factory`/`ragas.embeddings.OpenAIEmbeddings` and `ContextPrecision`/`ContextRecall`/`Faithfulness`/`AnswerRelevancy` from `ragas.metrics.collections`, kept in its own file specifically to avoid a namespace collision with `run_eval.py`'s legacy `ragas.metrics` imports of the same class names
- [x] 9.9 (extension, beyond original scope) `QA_PAIRS` extended with 3 hand-written cross-source questions, each requiring facts from two different source documents combined in one answer (not two chunks of the same file) — a harder, more realistic retrieval test than the original one-doc-per-question set

**Note (added 2026-08-22) — scope extensions during this lesson:** Beyond 9.2's original 8-pair scope, Kamran also drafted 3 unanswerable questions and 2 adversarial/prompt-injection questions for `QA_PAIRS`. After discussing that Ragas's four standard metrics (specifically Context Recall and Response Relevancy) aren't built to score a refusal-type `ground_truth` meaningfully — Context Recall decomposes `ground_truth` into factual claims to check against retrieved context, which doesn't apply to "this information is not available"; Response Relevancy can penalize a correct refusal for not resembling the original question — Kamran chose to keep the original 8-pair scope active and leave the 5 extra pairs commented out in `eval_dataset.py` rather than build the category-based metric routing a fair evaluation of them would need. Deferred, not abandoned — the commented block is intact and ready for a future pass.

**Note (added 2026-08-22) — modern Ragas API investigation:** Verified directly against the installed `ragas==0.4.3` package (not just documentation) that `evaluate()` and the legacy `ragas.metrics` classes (`ContextPrecision`, `ContextRecall`, `Faithfulness`, `ResponseRelevancy`) are deprecated in favor of `ragas.metrics.collections` + per-sample `await metric.ascore(...)` calls — a real architectural change (new classes don't inherit from the base class `evaluate()` checks for internally, so there's no batch runner for them), not just a renamed import. Also found a real bug in Ragas's own deprecation-warning text: it recommends `from ragas.metrics.collections import ResponseRelevancy`, which raises `ImportError` — the actual class there is named `AnswerRelevancy`. Built and ran `evaluate_pipeline_modern()` successfully; side-by-side comparison against the legacy path on the same 11-question dataset showed close but not identical scores (`context_precision` identical at 0.735; `context_recall` 0.894 vs. 0.924; `faithfulness` 0.959 vs. 0.982; `answer_relevancy` 0.819 vs. 0.818) — attributed to the two implementations using different internal prompting/structured-output mechanics to ask conceptually the same judge question, not a temperature difference (checked: legacy explicit `temperature=0`, modern default `temperature=0.01` — not a meaningful gap).

**Note (added 2026-08-22) — cross-source finding:** Comparing the original 8-question run against the 11-question run (8 + 3 cross-source) on the same pipeline: `context_precision` dropped from 0.885 to 0.735, `context_recall` dropped from a perfect 1.000 to 0.89–0.92, `answer_relevancy` dropped from 0.907 to ~0.82; `faithfulness` held steady or improved slightly. Real, named finding, not noise: the original one-document-per-question set was hitting a ceiling that doesn't reflect real usage, and this pipeline's retrieval is measurably weaker once a question's answer spans two source documents. Not chased further this lesson (e.g. isolating which specific cross-source question drove the drop via per-row scores) — flagged as a natural next step rather than required for closure.

**Note (added 2026-08-22) — closure:** 9.2's Done-When check was a real verification, not a rubber stamp — all 8 `ground_truth` answers were independently checked against the actual source documents (`hr_policies.md`, `employee_benefits_leave.md`, `travel_expense_policy.md`, `procurement_guidelines.md`, `product_catalog_faq.md`, `product_catalog_services_guide.md`, `corporate_gifts_price_list.pdf`, `company_overview.pdf`) and confirmed accurate. Two real bugs were caught and fixed before 9.4 was confirmed: a missing `import numpy as np` (would have raised `NameError` on the first run) and a typo in the function name (`evalutae_pipeline` → `evaluate_pipeline`), both caught by inspecting the actual applied code rather than trusting the "done" report. A real environment bug was also found and fixed: `ragas==0.4.3` imports a `langchain_community.chat_models.vertexai` path that current `langchain-community` releases have removed (split into a separate provider package) — fixed by pinning `langchain-community<0.4`. 9.7's concept check: Q2 (why the harness must call the real pipeline function) was correct and complete on the first attempt. Q1 (Faithfulness vs. Context Recall) correctly described what each metric measures individually but stopped short of naming what a specific combination (e.g. high recall + low faithfulness) tells you to actually go fix. Q3 (low precision + high recall) correctly diagnosed "retrieval finds the info but pulls noise alongside it" but stayed at "improve precision" rather than naming a concrete lever (reduce `k`, or tighten metadata filtering — both from Lesson 6). Both were sharpened live in conversation. **Kamran explicitly instructed marking the lesson complete without re-answering Q1/Q3** — recorded here explicitly as a deliberate deviation, the same way Lesson 2's and Lesson 6.6's early closures were recorded rather than silently treated as clean passes. The complete, sharpened reference answers are captured in `docs/Interview_QA.md`, per that file's own purpose (final distilled answer, not conversation history). Full writeup in `lesson-notes.md`.

## Lesson 10 — Common RAG Pitfalls & Optimization — ⏳ NOT STARTED (checklist ready)

*(Granular checklist prepared 2026-08-22, immediately after Lesson 9's closure, per the standing rule that the next lesson's checklist is ready before it begins. Not yet started — no items confirmed.)*

- [ ] 10.1 Common-pitfalls table discussed and understood — able to name the symptom, root cause, and fix for each of the seven pitfalls (lost in the middle, chunk-boundary information loss, embedding/query length mismatch, stale index, retrieval-generation mismatch, over-restrictive metadata filter, full re-embedding on every change), tying each back to the specific earlier lesson it connects to (Lesson 3 chunking, Lesson 6.5 query transformation, Lesson 7 grounding, Lesson 9 faithfulness) where relevant
- [ ] 10.2 Dependency check: no new package needed — Pinecone's hosted reranker uses the same `pinecone` client already installed since Lesson 5
- [ ] 10.3 `retrieval/reranker.py` created — `rerank(pc: Pinecone, query: str, scored_docs: list[tuple], top_n: int = 5) -> list[tuple]` written, calling `pc.inference.rerank(model="bge-reranker-v2-m3", query=query, documents=[doc.page_content for doc, _ in scored_docs], top_n=top_n, return_documents=False)` and mapping the reranked result indices back to the original `(Document, score)` tuples
- [ ] 10.4 Reranking wired into the pipeline as an opt-in path — retrieve wide (e.g. `retrieve(..., k=20)`), rerank narrow (`rerank(..., top_n=5)`), then feed the narrowed set into `format_docs()`/`answer_question()`; decide and implement where this switch lives (e.g. a new `use_reranking: bool = False` parameter on `answer_question()`, defaulting off so existing behavior and Lesson 9's baseline eval numbers stay unaffected unless explicitly enabled)
- [ ] 10.5 `run_rerank_demo.py` written at the project root (same entry-point convention as prior demo scripts) — runs one query through the pipeline twice, without and with reranking, printing both result sets' chunk order/sources side by side so the actual reordering effect is visible
- [ ] 10.6 Done-When check: demo run shows a visible difference in chunk order (or set) between reranked and non-reranked results for at least one query; optionally re-run Lesson 9's eval suite with reranking enabled vs. disabled to see whether `context_precision` actually moves — tying back to the roadmap's own "always measure, don't assume reranking helps" guidance
- [ ] 10.7 Concept check answered: why a reranker can afford to be slower and more accurate than the initial retriever (it scores ~20 candidates, not the whole index); why "lost in the middle" is a reason to rerank down to fewer chunks rather than just a prompt-wording problem; why reranking (this lesson) and query transformation (Lesson 6.5) together are what make this pipeline "Advanced RAG" per Lesson 2's own definition — one fixes what goes into retrieval, the other fixes what comes out of it

## Lesson 11 — Recap, Comparison, and What's Next — ⏳ NOT STARTED
*(granular checklist added when this lesson starts)*

---

**Rule for whoever (or whichever AI session) updates this file:** expand a lesson's placeholder into a full granular checklist (matching Lesson 1's density above) only when that lesson is actually started — not in advance. Check a box only after Kamran has explicitly confirmed that item is done. If in doubt, ask before checking. When starting a fresh chat or resuming this course, do a quick heading-count sanity check between this file and `docs/modern-rag-crash-course-roadmap.md` before trusting this file's lesson list is complete — this file is manually maintained and can drift, as it did once already. Also check `docs/Interview_QA.md` for prior assessed-question answers before re-asking a concept check this course has already covered.
